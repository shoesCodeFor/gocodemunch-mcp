# Phase 03: Provider Expansion and Retrieval Integration

Goal: integrate vector ingestion/retrieval into normal indexing flows and add vLLM as an alternate embedding provider while keeping Ollama as default.

## Auto-Run Tasks

- [x] Extend `src/internal/config/config.go` with vLLM embedding settings (`VLLM_BASE_URL`, `VLLM_MODEL`, optional auth key) and validation rules used when `EMBEDDING_PROVIDER=vllm`.
  - Completed: Added `Config` fields and defaults for `VLLM_BASE_URL`, `VLLM_MODEL`, and optional `VLLM_API_KEY`; expanded provider validation to accept `vllm`; and added provider-gated validation rules requiring a valid HTTP `VLLM_BASE_URL` and non-empty `VLLM_MODEL` when `EMBEDDING_PROVIDER=vllm`.
- [x] Add vLLM config coverage in `src/internal/config/config_test.go`, including provider switching and invalid-provider errors, then run `go test ./src/internal/config -run VLLM -count=1`.
  - Completed: Added `TestVectorConfigVLLMProviderSwitching` (vLLM <-> Ollama provider switching and provider-gated validation behavior) and `TestVectorConfigVLLMInvalidProviderError` (invalid provider message coverage), then ran `go test ./src/internal/config -run VLLM -count=1`.
- [x] Add `src/internal/orchestration/embeddings/vllm.go` implementing an OpenAI-compatible embedding client under the shared embedder interface.
  - Completed: Added `VLLMEmbedder` with a configurable constructor and options (`WithVLLMHTTPClient`, `WithVLLMBatchSize`), batched OpenAI-compatible `/embeddings` requests, optional Bearer auth support, deterministic response normalization by embedding `index`, timeout-aware request contexts, and retry-classified transport/HTTP error mapping aligned with shared vector error semantics.
- [x] Add `src/internal/orchestration/embeddings/vllm_test.go` for request mapping, timeout behavior, error mapping, and response normalization, then run `go test ./src/internal/orchestration/... -run VLLM -count=1`.
  - Completed: Added `vllm_test.go` coverage for request/response mapping (including `/embeddings` path and auth header), timeout retry classification, HTTP status error mapping (retryable and non-retryable), and deterministic normalization/error handling for indexed embedding responses; then ran `go test ./src/internal/orchestration/... -run VLLM -count=1`.
- [x] Add a deterministic chunking helper in `src/internal/orchestration` (or `src/internal/domain/indexing`) that converts indexed file content into stable chunks with source metadata.
  - Completed: Added `src/internal/orchestration/chunking.go` with deterministic line-window chunking (`buildDeterministicChunkMetadata`), stable chunk IDs hashed from repo/path/line-span/text, path normalization, language fallback, and cloned metadata fields; added `src/internal/orchestration/chunking_test.go` coverage for deterministic ordering/id stability, CRLF normalization, metadata correctness, and content-change ID updates; verified with `go test ./src/internal/orchestration/... -count=1`.
- [x] Update index create/update flows in `src/internal/orchestration/handlers_indexing.go` and related helpers so chunked content is embedded and upserted on every successful indexing operation.
  - Completed: Wired vector ingestion into `index_repo`, `index_folder` (full, incremental, and `changed_paths` fast-path), and `index_file` update flows; added shared helpers for candidate path normalization, local/remote file-content collection, deterministic chunk-to-record mapping, and embedding/upsert requests under repo namespaces; and kept indexing successful by treating provider/backend failures as non-fatal with explicit `"Vector upsert skipped: ..."` warnings on repo/folder responses.
  - Completed: Added `src/internal/orchestration/indexing_vector_upsert_test.go` coverage validating full and incremental vector upserts for `index_folder`, `index_file`, and `index_repo` (including local vs remote metadata source tagging), then ran `go test ./src/internal/orchestration/... -count=1`, `go test ./tests-go -count=1`, and `go test ./... -count=1`.
- [x] Update invalidate/delete/index-removal flows in `src/internal/orchestration` so vector entries are deleted whenever file or namespace state is removed.
  - Completed: Added vector deletion synchronization in `handlers_indexing.go` so incremental `index_repo`, `index_folder` (including `changed_paths` fast-path), and `index_file` delete stale chunk vectors for changed/removed files; persisted per-file chunk-ID metadata in index `ContextMetadata` under `_vector_chunk_ids_by_file` to enable deterministic deletes.
  - Completed: Updated full indexing flows to persist vector chunk-ID metadata for future delete reconciliation, and updated `handleInvalidateCache` to call vector `DeleteNamespace` for the repo while keeping index invalidation successful with warning-based degradation.
  - Completed: Expanded `src/internal/orchestration/indexing_vector_upsert_test.go` with coverage for deleted-file vector cleanup (local and remote), changed-file stale chunk deletion, and invalidate namespace deletion; then ran `go test ./src/internal/orchestration/... -count=1`, `go test ./tests-go -count=1`, and `go test ./... -count=1`.
- [x] Implement semantic retrieval execution in `src/internal/orchestration/handlers_retrieval.go` and add a hybrid ranker that combines lexical and vector scores with config-driven weights.
  - Completed: Extended `search_text` execution with retrieval modes (`lexical` default, plus `semantic` and `hybrid`) in `handlers_retrieval.go`; kept lexical response behavior intact while adding semantic vector query flow, mode validation, semantic result shaping, and grouped response envelopes that preserve existing top-level fields (`result_count`, `results`, `_meta`).
  - Completed: Added hybrid ranking helpers that merge lexical and semantic candidates, normalize per-source scores, and apply config-driven weights from `config.Config` (`VectorLexicalWeight`, `VectorSemanticWeight`) with optional per-call overrides (`lexical_weight`, `semantic_weight`) and deterministic tie-breaking.
  - Completed: Added vector weight config/env support in `src/internal/config/config.go` (`VECTOR_LEXICAL_WEIGHT`, `VECTOR_SEMANTIC_WEIGHT`) with validation coverage in `src/internal/config/config_test.go`.
  - Completed: Added `src/internal/orchestration/search_text_semantic_test.go` coverage for semantic ranking behavior, hybrid lexical/vector blending, weight override behavior, and invalid retrieval mode handling; ran `go test ./src/internal/config -count=1` and `go test ./src/internal/orchestration -run SearchText -count=1` successfully.
  - Note: `go test ./... -count=1` currently fails in pre-existing storage concurrency coverage at `src/internal/storage/sqlite_store_test.go::TestSQLiteIndexStoreConcurrentSaveLoad` with `database is locked (261)`; targeted retrieval/config suites pass.
- [x] Update MCP contracts in `src/internal/orchestration/tooldefs.go` (and related tests) to expose semantic and hybrid retrieval without breaking current tool response envelopes.
  - Completed: Updated `search_text` tool input schema in `tooldefs.go` to expose `retrieval_mode` enum (`lexical`, `semantic`, `hybrid`) with default `lexical`, plus optional `lexical_weight`/`semantic_weight` numeric overrides for hybrid ranking.
  - Completed: Added `tool_contracts_test.go` coverage asserting `search_text` retrieval-mode enum/default, hybrid weight property types, and stable required fields (`repo`, `query`) to preserve the existing response contract surface.
  - Completed: Ran `go test ./src/internal/orchestration -run 'SearchTextContract|SearchText|SearchSymbolsLanguageEnumDefaultsToSupportedLanguages|ConfiguredLanguagesFilterEnumAndDisableSearchColumns' -count=1` and `go test ./src/internal/orchestration -count=1`.
- [x] Add resilience controls (timeouts, retries, bounded batch sizes, graceful degradation) in retrieval/index paths with structured logs for provider/backend failure cases.
  - Completed: Added shared vector resilience helpers in `src/internal/orchestration/vector_resilience.go` that apply per-operation timeout budgets (`VectorQueryTimeoutMS`), retry retryable provider/backend failures with backoff, and emit structured log events (`vector_operation_failure` / `vector_operation_recovered`) with operation/attempt/backend/provider context.
  - Completed: Updated indexing vector flows in `src/internal/orchestration/handlers_indexing.go` to use resilient embedding/upsert/delete operations and bounded batching (`defaultVectorUpsertBatchSize=128`, `defaultVectorDeleteBatchSize=256`), while preserving warning-based graceful degradation semantics.
  - Completed: Updated retrieval semantic/hybrid flow in `src/internal/orchestration/handlers_retrieval.go` to execute semantic embed/query with retries/timeouts and gracefully degrade to lexical results on dependency/provider/backend failures, annotating `_meta` with `retrieval_mode`, `effective_retrieval_mode`, `degraded`, and `degrade_reason`.
  - Completed: Added/expanded tests in `src/internal/orchestration/search_text_semantic_test.go` and `src/internal/orchestration/indexing_vector_upsert_test.go` for semantic fallback behavior, retry recovery, and bounded upsert batching; ran `go test ./src/internal/orchestration -run 'TestSearchTextSemanticModeUsesVectorRanking|TestSearchTextHybridModeCombinesLexicalAndVectorScores|TestSearchTextSemanticFallsBackToLexicalOnVectorFailure|TestSearchTextRejectsUnknownRetrievalMode' -count=1`, `go test ./src/internal/orchestration -run 'TestIndexFolderVectorUpsertRetriesRetryableFailure|TestIndexFolderVectorUpsertBatchesAreBounded|TestIndexFolderFullUpsertsVectorChunks|TestIndexFolderChangedPathsUpsertsModifiedAndAddedFiles|TestIndexFileUpsertsOnlyOnContentChange|TestIndexRepoUpsertsRemoteVectorChunks|TestInvalidateCacheDeletesVectorNamespace' -count=1`, `go test ./src/internal/orchestration -count=1`, and `go test ./tests-go -count=1`.
  - Note: `go test ./... -count=1` still fails in pre-existing storage concurrency coverage at `src/internal/storage/sqlite_store_test.go::TestSQLiteIndexStoreConcurrentSaveLoad` with `database is locked (261)`; orchestration/tests-go suites pass.
- [ ] Add integration tests in `src/internal/orchestration` and `tests-go` for index-to-vector sync, provider switching, semantic quality, and hybrid ranking behavior.
- [ ] Run `go test ./... -count=1`, then run smoke retrieval with `EMBEDDING_PROVIDER=ollama` and `EMBEDDING_PROVIDER=vllm` (when available) across both `sqlite` and `qdrant` backends.
