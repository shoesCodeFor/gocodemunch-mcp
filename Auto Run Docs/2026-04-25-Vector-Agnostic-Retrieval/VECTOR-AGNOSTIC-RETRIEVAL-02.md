# Phase 02: Qdrant Parity and Local Dev Stack

Goal: add a Qdrant backend with contract parity to sqlite and make backend switching entirely config-driven for local development.

## Auto-Run Tasks

- [x] Create `docker-compose.vector.yml` at repo root with a `qdrant` service, deterministic exposed ports, named volume persistence, and a healthcheck command suitable for automated waits.
  - Completed: Added `docker-compose.vector.yml` with `qdrant/qdrant:latest`, fixed host port mappings (`6333`, `6334`), named volume `gocodemunch_qdrant_storage`, and a `/readyz` bash TCP healthcheck validated with `docker compose -f docker-compose.vector.yml up -d --wait`.
- [x] Add `make vector-up`, `make vector-down`, and `make vector-health` targets in `Makefile` that use `docker-compose.vector.yml` non-interactively.
  - Completed: Added targets plus `make help` wiring; `vector-up` now runs `docker compose -f docker-compose.vector.yml up -d --wait --quiet-pull`, `vector-health` prints JSON status and asserts `"Health":"healthy"`, and `vector-down` performs non-interactive teardown with `down --remove-orphans`.
- [x] Extend `src/internal/config/config.go` with Qdrant settings (`QDRANT_URL`, `QDRANT_API_KEY`, `QDRANT_COLLECTION`) and validation for required values when `VECTOR_BACKEND=qdrant`.
  - Completed: Added `Config` fields and defaults for `QdrantURL`, `QdrantAPIKey`, and `QdrantCollection`; extended `VECTOR_BACKEND` validation to allow `qdrant`; wired env overrides for `QDRANT_URL`/`QDRANT_API_KEY`/`QDRANT_COLLECTION`; and added backend-gated required-value checks for `QDRANT_URL` and `QDRANT_COLLECTION` when `VECTOR_BACKEND=qdrant` with actionable error messages.
- [x] Add Qdrant config tests in `src/internal/config/config_test.go` for defaults, env overrides, and validation errors, then run `go test ./src/internal/config -run Qdrant -count=1`.
  - Completed: Added `TestQdrantConfigDefaults`, `TestQdrantConfigEnvOverrides`, and `TestQdrantConfigValidationErrors` in `src/internal/config/config_test.go`; validated with `go test ./src/internal/config -run Qdrant -count=1` (pass).
- [x] Add `src/internal/storage/vector/qdrant/adapter.go` to implement collection bootstrap, `Upsert`, and `Query` compatible with shared vector contracts.
  - Completed: Added a new `qdrant.Adapter` with validated constructor inputs (`baseURL`, API key, collection), lazy collection bootstrap that creates/validates a cosine collection using first-seen embedding dimensions, namespace-aware `Upsert` batching to `/collections/{collection}/points`, and `Query` via namespace-filtered Qdrant search that normalizes payload/metadata into shared vector contracts with deterministic tie-break sorting compatible with sqlite ordering.
- [x] Complete `src/internal/storage/vector/qdrant/adapter.go` with `Delete`, `DeleteNamespace`, and `Health` methods plus explicit transport/error mapping.
  - Completed: Implemented namespace-safe `Delete` (ID lookup + namespace filtering before delete), filter-based `DeleteNamespace` with exact pre-count cardinality reporting, and `Health` checks with backend diagnostics metadata (`backend`, `base_url`, `collection`, `collection_exists`, `vector_dimension` when available). Added explicit Qdrant transport/error mapping via typed retryable `qdrantError` wrappers for HTTP transport/status failures and request/response processing paths; validated with new focused tests in `src/internal/storage/vector/qdrant/adapter_delete_health_test.go` and `go test ./src/internal/storage/vector/qdrant -count=1`, plus regression pass `go test ./src/internal/storage/... -count=1`.
- [x] Add `src/internal/storage/vector/qdrant/adapter_test.go` for deterministic tie ordering and error translation behavior, then run `go test ./src/internal/storage/... -run Qdrant -count=1`.
  - Completed: Added `src/internal/storage/vector/qdrant/adapter_test.go` with `TestQdrantQueryDeterministicTieOrdering` (stable tie ordering across repeated query responses with different server ordering) and public-query error translation coverage for transport failures plus retryable/non-retryable HTTP status mapping. Validated with `go test ./src/internal/storage/... -run Qdrant -count=1` (pass).
- [x] Update backend factory wiring in `src/server/server.go` (or existing factory module) so sqlite and Qdrant are selected by `VECTOR_BACKEND` without code edits.
  - Completed: Extended server vector backend factory wiring to support `VECTOR_BACKEND=qdrant` via `qdrant.NewAdapter(QDRANT_URL, QDRANT_API_KEY, QDRANT_COLLECTION)`, kept sqlite as default/fallback, updated unsupported-backend guidance to include both valid options, and added `server_dependencies_test` coverage verifying Qdrant adapter selection and normalized config propagation.
- [x] Add contract parity tests in `src/internal/storage/vector/contract_parity_test.go` that run identical cases against sqlite and Qdrant adapters.
  - Completed: Added `src/internal/storage/vector/contract_parity_test.go` with shared backend contract suites (`CRUDLifecycle`, `Validation`) executed against both sqlite and Qdrant adapters using identical request/assertion flows. Implemented an in-test fake Qdrant HTTP server to exercise collection bootstrap, upsert, query ranking, delete/delete-namespace, and health behavior without external services. Verified with `go test ./src/internal/storage/vector/... -count=1` (pass).
- [x] Add integration tests in `tests-go` for live Qdrant lifecycle operations (upsert/query/delete/namespace-delete) and skip cleanly when `QDRANT_URL` is unset.
  - Completed: Added `tests-go/qdrant_live_integration_test.go` with `TestQdrantLiveLifecycleOperations`, a live Qdrant HTTP lifecycle integration that provisions an isolated collection per run, performs upsert/query/delete/delete-namespace checks with namespace isolation assertions, and cleanly skips via `t.Skip` when `QDRANT_URL` is unset. Validated with `go test ./tests-go -run QdrantLiveLifecycleOperations -count=1 -v` and `go test ./tests-go -count=1` (pass; lifecycle test skips when env is unset).
- [ ] Run `make vector-up`, `go test ./... -count=1`, `VECTOR_BACKEND=sqlite make vector-smoke`, and `VECTOR_BACKEND=qdrant make vector-smoke`, then run `make vector-down`.
